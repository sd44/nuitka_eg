Metadata-Version: 2.4
Name: installer
Version: 0.1.0
Summary: Default template for PDM package
Author-email: sd44 <sd44sd44@yeah.net>
License: MIT
Requires-Python: >=3.12
Description-Content-Type: text/markdown
Requires-Dist: pyside6>=6.11.1

## 本地编译命令 
```bash
pdm install
. .venv/bin/activate.fish
python -m nuitka --onefile --enable-plugin=pyside6 --windows-disable-console --lto=yes --remove-output --output-dir=release --company-name="My Company" main.py
```
## 转换为requirements.txt

### 导出所有依赖（包括 dev 依赖）
uv pip compile pyproject.toml --all-extras --output-file requirements.txt


### 仅导出主依赖（不含 dev-dependencies）
uv pip compile pyproject.toml --output-file requirements.txt

### 如果你想包含开发依赖（等效于 pdm 的默认行为）
uv pip compile pyproject.toml --extra=dev --output-file requirements-dev.txt

## PySide6 LICENSE

在软件文档或“关于”页面中声明：

+ 本产品使用了 PySide6 / Qt
+ Qt is licensed under LGPL v3
+ 提供 Qt 官网链接：<https://www.qt.io/>
+ 允许用户替换 Qt 库（技术上已满足）： Nuitka 打包后，Qt 相关 DLL（如 Qt6Core.dll, Qt6Gui.dll）是独立文件（--standalone）或可被替换的（--onefile 虽打包但法律上视为动态链接）
        你不需要提供你的源代码

    📝 示例声明（放在“关于”对话框或 LICENSE.txt 中）：
    文本

This application uses PySide6 (Qt for Python).

Qt is licensed under the GNU Lesser General Public License (LGPL) version 3.

For details, see: <https://www.qt.io/licensing/>

## build.yml

见 `.github/workflows/build.yml`
